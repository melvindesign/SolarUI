---
title: Badge
description: Displays a badge or a component that looks like a badge.
---

import { Badge } from '@/components/ui/badge'
import { ComponentPreview, ComponentPreviewRow } from '@/components/component-preview'
import { Bell, Fire } from '@phosphor-icons/react/dist/ssr'
import { Tabs } from 'nextra/components'

# Badge

Displays a badge or a component that looks like a badge.

<ComponentPreview>
  <Badge>Badge</Badge>
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
import { Badge } from "@/components/ui/badge"
```

```tsx
<Badge>Badge</Badge>
```

## Examples

### Variants

<ComponentPreviewRow>
  <Badge>Default</Badge>
  <Badge variant="primary">Primary</Badge>
  <Badge variant="secondary">Secondary</Badge>
  <Badge variant="destructive">Destructive</Badge>
  <Badge variant="outline">Outline</Badge>
  <Badge variant="ghost">Ghost</Badge>
</ComponentPreviewRow>

```tsx
<Badge>Default</Badge>
<Badge variant="primary">Primary</Badge>
<Badge variant="secondary">Secondary</Badge>
<Badge variant="destructive">Destructive</Badge>
<Badge variant="outline">Outline</Badge>
<Badge variant="ghost">Ghost</Badge>
```

### With icon

<ComponentPreviewRow>
  <Badge><Fire data-icon="inline-start" /> Hot</Badge>
  <Badge variant="secondary"><Bell data-icon="inline-start" /> 12 notifications</Badge>
  <Badge variant="outline">New <Fire data-icon="inline-end" /></Badge>
</ComponentPreviewRow>

```tsx
import { Fire, Bell } from "lucide-react"

<Badge>
  <Fire data-icon="inline-start" />
  Hot
</Badge>

<Badge variant="secondary">
  <Bell data-icon="inline-start" />
  12 notifications
</Badge>
```

### As link

<ComponentPreview>
  <Badge asChild variant="outline">
    <a href="#">View changelog</a>
  </Badge>
</ComponentPreview>

```tsx
<Badge asChild variant="outline">
  <a href="#">View changelog</a>
</Badge>
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add https://solar-ui.com/r/badge.json
```

---

### Manual

Copy and paste the following code into your project:

```tsx filename="components/ui/badge.tsx" copy
import { cva, type VariantProps } from "class-variance-authority"
import { Slot } from "radix-ui"
import * as React from "react"

import { cn } from "@/lib/utils"

const badgeVariants = cva(
  "inline-flex w-fit shrink-0 items-center justify-center gap-1 overflow-hidden rounded-full border border-transparent px-2 py-0.5 text-xs font-medium whitespace-nowrap transition-[color,box-shadow] focus-visible:border-default-8 focus-visible:ring-[3px] focus-visible:ring-default-7 aria-invalid:border-error-8 aria-invalid:ring-error-7 [&>svg]:pointer-events-none [&>svg]:size-3",
  {
    variants: {
      variant: {
        default: "bg-default-12 text-default-1 [a&]:hover:bg-default-11",
        primary: "bg-brand-9 text-brand-1 [a&]:hover:bg-brand-10",
        secondary: "bg-default-3 text-default-12 [a&]:hover:bg-default-4",
        destructive: "bg-error-9 text-error-1 focus-visible:ring-error-7 [a&]:hover:bg-error-10",
        outline: "border-default-6 text-default-12 [a&]:hover:bg-default-3",
        ghost: "text-default-12 [a&]:hover:bg-default-3",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
)

function Badge({
  className,
  variant = "default",
  asChild = false,
  ...props
}: React.ComponentProps<"span"> &
  VariantProps<typeof badgeVariants> & { asChild?: boolean }) {
  const Comp = asChild ? Slot.Root : "span"

  return (
    <Comp
      data-slot="badge"
      data-variant={variant}
      className={cn(badgeVariants({ variant }), className)}
      {...props}
    />
  )
}

export { Badge, badgeVariants }
```

</Tabs.Tab>

<Tabs.Tab>

### Badge

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"default" \| "primary" \| "secondary" \| "destructive" \| "outline" \| "ghost"` | `"default"` | The visual style of the badge. |
| `asChild` | `boolean` | `false` | When `true`, renders its child as the root element using Radix UI's `Slot`. |
| `className` | `string` | — | Additional CSS classes to merge. |

</Tabs.Tab>

</Tabs>
