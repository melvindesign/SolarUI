---
title: Alert
description: Displays a callout for user attention.
---

import { Alert, AlertTitle, AlertDescription } from '@/components/ui/alert'
import { ComponentPreview, ComponentPreviewRow } from '@/components/component-preview'
import { Info, TriangleAlert } from 'lucide-react'
import { Tabs } from 'nextra/components'

# Alert

Displays a callout for user attention.

<ComponentPreview>
  <Alert className="max-w-sm">
    <Info className="h-4 w-4" />
    <AlertTitle>Heads up</AlertTitle>
    <AlertDescription>
      You can add components to your app using the CLI.
    </AlertDescription>
  </Alert>
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
import { Alert, AlertTitle, AlertDescription } from "@/components/ui/alert"
```

```tsx
<Alert>
  <AlertTitle>Heads up</AlertTitle>
  <AlertDescription>You can add components using the CLI.</AlertDescription>
</Alert>
```

## Examples

### Default

<ComponentPreview>
  <Alert className="max-w-sm">
    <Info className="h-4 w-4" />
    <AlertTitle>Information</AlertTitle>
    <AlertDescription>
      Your session will expire in 30 minutes.
    </AlertDescription>
  </Alert>
</ComponentPreview>

```tsx
import { Info } from "lucide-react"

<Alert>
  <Info className="h-4 w-4" />
  <AlertTitle>Information</AlertTitle>
  <AlertDescription>Your session will expire in 30 minutes.</AlertDescription>
</Alert>
```

### Destructive

<ComponentPreview>
  <Alert variant="destructive" className="max-w-sm">
    <TriangleAlert className="h-4 w-4" />
    <AlertTitle>Error</AlertTitle>
    <AlertDescription>
      Your session has expired. Please log in again.
    </AlertDescription>
  </Alert>
</ComponentPreview>

```tsx
import { TriangleAlert } from "lucide-react"

<Alert variant="destructive">
  <TriangleAlert className="h-4 w-4" />
  <AlertTitle>Error</AlertTitle>
  <AlertDescription>Your session has expired. Please log in again.</AlertDescription>
</Alert>
```

### Without icon

<ComponentPreview>
  <Alert className="max-w-sm">
    <AlertTitle>Note</AlertTitle>
    <AlertDescription>
      This action cannot be undone once confirmed.
    </AlertDescription>
  </Alert>
</ComponentPreview>

```tsx
<Alert>
  <AlertTitle>Note</AlertTitle>
  <AlertDescription>This action cannot be undone once confirmed.</AlertDescription>
</Alert>
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add https://solar-ui.com/r/alert.json
```

---

### Manual

Copy and paste the following code into your project:

```tsx filename="components/ui/alert.tsx" copy
import { cva, type VariantProps } from "class-variance-authority"
import * as React from "react"

import { cn } from "@/lib/utils"

const alertVariants = cva("grid gap-0.5 rounded-lg border px-2.5 py-2 text-left text-sm has-data-[slot=alert-action]:relative has-data-[slot=alert-action]:pr-18 has-[>svg]:grid-cols-[auto_1fr] has-[>svg]:gap-x-2 *:[svg]:row-span-2 *:[svg]:translate-y-0.5 *:[svg]:text-current *:[svg:not([class*='size-'])]:size-4 group/alert relative w-full", {
  variants: {
    variant: {
      default: "bg-default-2 text-default-12",
      destructive: "text-error-11 bg-default-2 *:data-[slot=alert-description]:text-error-11 *:[svg]:text-current",
    },
  },
  defaultVariants: {
    variant: "default",
  },
})

function Alert({
  className,
  variant,
  ...props
}: React.ComponentProps<"div"> & VariantProps<typeof alertVariants>) {
  return (
    <div
      data-slot="alert"
      role="alert"
      className={cn(alertVariants({ variant }), className)}
      {...props}
    />
  )
}

function AlertTitle({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="alert-title"
      className={cn(
        "font-medium group-has-[>svg]/alert:col-start-2 [&_a]:underline [&_a]:underline-offset-3 [&_a]:hover:text-default-12",
        className
      )}
      {...props}
    />
  )
}

function AlertDescription({
  className,
  ...props
}: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="alert-description"
      className={cn(
        "text-default-11 text-sm text-balance md:text-pretty [&_p:not(:last-child)]:mb-4 [&_a]:underline [&_a]:underline-offset-3 [&_a]:hover:text-default-12",
        className
      )}
      {...props}
    />
  )
}

function AlertAction({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="alert-action"
      className={cn("absolute top-2 right-2", className)}
      {...props}
    />
  )
}

export { Alert, AlertAction, AlertDescription, AlertTitle }
```

</Tabs.Tab>

<Tabs.Tab>

### Alert

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"default" \| "destructive"` | `"default"` | The visual style of the alert. |
| `className` | `string` | — | Additional CSS classes to merge. |

### AlertTitle

Renders as an `<h5>`. Accepts all standard HTML heading attributes and `className`.

### AlertDescription

Renders as a `<div>`. Accepts all standard HTML div attributes and `className`.

</Tabs.Tab>

</Tabs>
