---
title: Line Chart
description: A chart that connects data points with a line, useful for visualizing trends over time.
---

import { LineChartDefault, LineChartWithDots } from '@/components/charts/line-chart'
import { ComponentPreview } from '@/components/component-preview'
import { Tabs } from 'nextra/components'

# Line Chart

A chart that connects data points with a line, useful for visualizing trends over time.

<ComponentPreview>
  <LineChartDefault />
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
"use client"

import { Line, LineChart, CartesianGrid, XAxis } from "recharts"
import {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
} from "@/components/ui/chart"
```

```tsx
<ChartContainer config={chartConfig} className="h-64 w-full">
  <LineChart data={chartData}>
    <CartesianGrid vertical={false} />
    <XAxis dataKey="month" />
    <ChartTooltip content={<ChartTooltipContent />} />
    <Line dataKey="desktop" stroke="var(--color-desktop)" strokeWidth={2} dot={false} />
  </LineChart>
</ChartContainer>
```

## Examples

### With dots

<ComponentPreview>
  <LineChartWithDots />
</ComponentPreview>

```tsx
<Line
  dataKey="desktop"
  stroke="var(--color-desktop)"
  strokeWidth={2}
  dot={{ r: 4 }}
  activeDot={{ r: 6 }}
/>
```

### Linear interpolation

```tsx
<Line type="linear" dataKey="desktop" ... />
```

Available `type` values: `"basis"`, `"bumpX"`, `"bumpY"`, `"bump"`, `"linear"`, `"monotone"`, `"natural"`, `"step"`, `"stepAfter"`, `"stepBefore"`.

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add http://localhost:3000/r/chart-line.json
```

---

### Manual

**1. Install the dependency:**

```bash filename="terminal" copy
npm install recharts
```

**2. Copy `components/ui/chart.tsx` into your project.**

</Tabs.Tab>

<Tabs.Tab>

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `string` | `"linear"` | Interpolation curve type. |
| `strokeWidth` | `number` | `1` | Line thickness. |
| `dot` | `boolean \| object` | `true` | Show/style dot markers. |
| `activeDot` | `boolean \| object` | `true` | Dot style on hover. |

</Tabs.Tab>

</Tabs>
