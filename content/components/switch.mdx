---
title: Switch
description: A control that allows the user to toggle between checked and not checked.
---

import { Label } from '@/components/ui/label'
import { Switch } from '@/components/ui/switch'
import { Field, FieldContent, FieldDescription, FieldError, FieldLabel } from '@/components/ui/field'
import { ComponentPreview } from '@/components/component-preview'
import { Tabs } from 'nextra/components'

# Switch

A control that allows the user to toggle between checked and not checked.

<ComponentPreview>
  <div className="flex items-center gap-2">
    <Switch id="airplane-mode" />
    <Label htmlFor="airplane-mode">Airplane Mode</Label>
  </div>
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
import { Switch } from "@/components/ui/switch"
import { Label } from "@/components/ui/label"
```

```tsx
<div className="flex items-center gap-2">
  <Switch id="my-switch" />
  <Label htmlFor="my-switch">Enable feature</Label>
</div>
```

## Examples

### Checked by default

<ComponentPreview>
  <div className="flex items-center gap-2">
    <Switch id="notifications" defaultChecked />
    <Label htmlFor="notifications">Notifications</Label>
  </div>
</ComponentPreview>

```tsx
<div className="flex items-center gap-2">
  <Switch id="notifications" defaultChecked />
  <Label htmlFor="notifications">Notifications</Label>
</div>
```

### Disabled

<ComponentPreview>
  <div className="flex flex-col gap-3">
    <div className="flex items-center gap-2">
      <Switch id="disabled-off" disabled />
      <Label htmlFor="disabled-off" className="opacity-50">Off (disabled)</Label>
    </div>
    <div className="flex items-center gap-2">
      <Switch id="disabled-on" disabled defaultChecked />
      <Label htmlFor="disabled-on" className="opacity-50">On (disabled)</Label>
    </div>
  </div>
</ComponentPreview>

```tsx
<Switch disabled />
<Switch disabled defaultChecked />
```

### With description

<ComponentPreview>
  <Field orientation="horizontal">
    <Switch id="desc-notifications" />
    <FieldContent>
      <FieldLabel htmlFor="desc-notifications">Push notifications</FieldLabel>
      <FieldDescription>Receive alerts for new messages and updates.</FieldDescription>
    </FieldContent>
  </Field>
</ComponentPreview>

```tsx
<Field orientation="horizontal">
  <Switch id="notifications" />
  <FieldContent>
    <FieldLabel htmlFor="notifications">Push notifications</FieldLabel>
    <FieldDescription>Receive alerts for new messages and updates.</FieldDescription>
  </FieldContent>
</Field>
```

### Invalid

<ComponentPreview>
  <Field orientation="horizontal" data-invalid>
    <Switch id="invalid-terms" aria-invalid />
    <FieldContent>
      <FieldLabel htmlFor="invalid-terms">Accept terms and conditions</FieldLabel>
      <FieldError>You must accept the terms to continue.</FieldError>
    </FieldContent>
  </Field>
</ComponentPreview>

```tsx
<Field orientation="horizontal" data-invalid>
  <Switch id="terms" aria-invalid />
  <FieldContent>
    <FieldLabel htmlFor="terms">Accept terms and conditions</FieldLabel>
    <FieldError>You must accept the terms to continue.</FieldError>
  </FieldContent>
</Field>
```

### Card

<ComponentPreview>
  <div className="grid w-full max-w-sm gap-3">
    <FieldLabel>
      <Field orientation="horizontal">
        <Switch id="card-email" defaultChecked />
        <FieldContent>
          <FieldLabel htmlFor="card-email">Email notifications</FieldLabel>
          <FieldDescription>Get notified when someone mentions you.</FieldDescription>
        </FieldContent>
      </Field>
    </FieldLabel>
    <FieldLabel>
      <Field orientation="horizontal">
        <Switch id="card-marketing" />
        <FieldContent>
          <FieldLabel htmlFor="card-marketing">Marketing emails</FieldLabel>
          <FieldDescription>Receive emails about new products and features.</FieldDescription>
        </FieldContent>
      </Field>
    </FieldLabel>
  </div>
</ComponentPreview>

```tsx
<div className="grid gap-3">
  <FieldLabel>
    <Field orientation="horizontal">
      <Switch id="email" defaultChecked />
      <FieldContent>
        <FieldLabel htmlFor="email">Email notifications</FieldLabel>
        <FieldDescription>Get notified when someone mentions you.</FieldDescription>
      </FieldContent>
    </Field>
  </FieldLabel>
  <FieldLabel>
    <Field orientation="horizontal">
      <Switch id="marketing" />
      <FieldContent>
        <FieldLabel htmlFor="marketing">Marketing emails</FieldLabel>
        <FieldDescription>Receive emails about new products and features.</FieldDescription>
      </FieldContent>
    </Field>
  </FieldLabel>
</div>
```

### Size

<ComponentPreview>
  <div className="flex items-center gap-4">
    <div className="flex items-center gap-2">
      <Switch id="size-compact" size="compact" defaultChecked />
      <Label htmlFor="size-compact">Compact</Label>
    </div>
    <div className="flex items-center gap-2">
      <Switch id="size-default" size="default" defaultChecked />
      <Label htmlFor="size-default">Default</Label>
    </div>
  </div>
</ComponentPreview>

```tsx
<Switch size="compact" />
<Switch size="default" />
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add @solar-ui/switch
```

---

### Manual

**1. Install the following dependency:**

```bash filename="terminal" copy
npm install @radix-ui/react-switch
```

**2. Copy and paste the following code into your project:**

```tsx filename="components/ui/switch.tsx" copy
"use client"

import { Switch as SwitchPrimitive } from "radix-ui"
import * as React from "react"

import { cn } from "@/lib/utils"

function Switch({
  className,
  size = "default",
  ...props
}: React.ComponentProps<typeof SwitchPrimitive.Root> & {
  size?: "compact" | "default"
}) {
  return (
    <SwitchPrimitive.Root
      data-slot="switch"
      data-size={size}
      className={cn(
        "data-[state=checked]:bg-brand-9 data-[state=unchecked]:bg-default-3 focus-visible:border-default-8 focus-visible:ring-default-7 aria-invalid:ring-error-7 aria-invalid:border-error-8 shrink-0 rounded-full border border-transparent focus-visible:ring-3 aria-invalid:ring-3 data-[size=default]:h-[18.4px] data-[size=default]:w-[32px] data-[size=compact]:h-[14px] data-[size=compact]:w-[24px] peer group/switch relative inline-flex items-center transition-all outline-none after:absolute after:-inset-x-3 after:-inset-y-2 data-disabled:cursor-not-allowed data-disabled:opacity-50",
        className
      )}
      {...props}
    >
      <SwitchPrimitive.Thumb
        data-slot="switch-thumb"
        className="bg-default-1 rounded-full group-data-[size=default]/switch:size-4 group-data-[size=compact]/switch:size-3 group-data-[size=default]/switch:data-[state=checked]:translate-x-[calc(100%-2px)] group-data-[size=compact]/switch:data-[state=checked]:translate-x-[calc(100%-2px)] group-data-[size=default]/switch:data-[state=unchecked]:translate-x-0 group-data-[size=compact]/switch:data-[state=unchecked]:translate-x-0 pointer-events-none block ring-0 transition-transform"
      />
    </SwitchPrimitive.Root>
  )
}

export { Switch }
```

</Tabs.Tab>

<Tabs.Tab>

### Switch

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean` | — | Controlled checked state of the switch. |
| `defaultChecked` | `boolean` | `false` | Initial checked state (uncontrolled). |
| `onCheckedChange` | `(checked: boolean) => void` | — | Callback when the checked state changes. |
| `disabled` | `boolean` | `false` | When `true`, prevents interaction and applies reduced opacity. |
| `required` | `boolean` | `false` | Makes the switch required in a form context. |
| `size` | `"default" \| "compact"` | `"default"` | Controls the size of the switch. |

</Tabs.Tab>

</Tabs>
