---
title: Radar Chart
description: A spider/web chart that displays multivariate data as a polygon on a radial grid.
---

import { RadarChartDefault, RadarChartSingle } from '@/components/charts/radar-chart'
import { ComponentPreview } from '@/components/component-preview'
import { Tabs } from 'nextra/components'

# Radar Chart

A spider/web chart that displays multivariate data as a polygon on a radial grid.

<ComponentPreview>
  <RadarChartDefault />
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
"use client"

import { Radar, RadarChart, PolarGrid, PolarAngleAxis } from "recharts"
import {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
} from "@/components/ui/chart"
```

```tsx
<ChartContainer config={chartConfig} className="h-72 w-full">
  <RadarChart data={chartData}>
    <PolarGrid />
    <PolarAngleAxis dataKey="month" />
    <ChartTooltip content={<ChartTooltipContent />} />
    <Radar
      dataKey="desktop"
      fill="var(--color-desktop)"
      fillOpacity={0.3}
      stroke="var(--color-desktop)"
    />
  </RadarChart>
</ChartContainer>
```

## Examples

### Single series with dots

<ComponentPreview>
  <RadarChartSingle />
</ComponentPreview>

```tsx
<Radar
  dataKey="desktop"
  fill="var(--color-desktop)"
  fillOpacity={0.4}
  dot={{ r: 4, fillOpacity: 1 }}
/>
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add https://solar-ui.com/r/chart-radar.json
```

---

### Manual

**1. Install the dependency:**

```bash filename="terminal" copy
npm install recharts
```

**2. Copy `components/ui/chart.tsx` into your project.**

</Tabs.Tab>

<Tabs.Tab>

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `dataKey` | `string` | — | Key in data objects to plot. |
| `fill` | `string` | — | Fill color for the polygon area. |
| `fillOpacity` | `number` | `0.6` | Transparency of the fill. |
| `stroke` | `string` | — | Color of the polygon outline. |
| `dot` | `boolean \| object` | `false` | Show dot markers at each vertex. |

</Tabs.Tab>

</Tabs>
