---
title: Body
description: Paragraph text component for readable body content.
---

import { Body } from '@/components/ui/body'
import { ComponentPreview } from '@/components/component-preview'
import { Tabs } from 'nextra/components'

# Body

Paragraph text component for readable body content. Renders as `<p>` by default, and as `<small>` for the `compact` variant.

<ComponentPreview>
  <div className="flex flex-col gap-4">
    <Body>The quick brown fox jumps over the lazy dog.</Body>
    <Body variant="compact">The quick brown fox jumps over the lazy dog.</Body>
  </div>
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
import { Body } from "@/components/ui/body"
```

```tsx
<Body>The quick brown fox jumps over the lazy dog.</Body>
```

## Examples

### Default vs Compact

<ComponentPreview>
  <div className="flex flex-col gap-4 max-w-md">
    <div>
      <p className="text-xs text-default-11 mb-1">Default (16px) — renders as &lt;p&gt;</p>
      <Body>The quick brown fox jumps over the lazy dog. Pack my box with five dozen liquor jugs.</Body>
    </div>
    <div>
      <p className="text-xs text-default-11 mb-1">Compact (14.22px) — renders as &lt;small&gt;</p>
      <Body variant="compact">The quick brown fox jumps over the lazy dog. Pack my box with five dozen liquor jugs.</Body>
    </div>
  </div>
</ComponentPreview>

```tsx
<Body>Default body text</Body>
<Body variant="compact">Compact body text</Body>
```

### Custom element with asChild

<ComponentPreview>
  <Body asChild>
    <span>Body text rendered as a span</span>
  </Body>
</ComponentPreview>

```tsx
<Body asChild>
  <span>Body text rendered as a span</span>
</Body>
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add https://solar-ui.com/r/body.json
```

---

### Manual

Copy and paste the following code into your project:

```tsx filename="components/ui/body.tsx" copy
import { cva, type VariantProps } from "class-variance-authority"
import { Slot } from "radix-ui"
import * as React from "react"

import { cn } from "@/lib/utils"

const bodyVariants = cva(
  "font-sans text-default-12",
  {
    variants: {
      variant: {
        default: "text-body tracking-body font-normal",
        compact: "text-body-compact tracking-body font-normal",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
)

const variantTagMap = {
  default: "p",
  compact: "small",
} as const

type BodyProps = React.HTMLAttributes<HTMLElement> &
  VariantProps<typeof bodyVariants> & { asChild?: boolean }

function Body({ className, variant = "default", asChild = false, ...props }: BodyProps) {
  const Comp = (asChild ? Slot.Root : variantTagMap[variant ?? "default"]) as React.ElementType
  return (
    <Comp
      data-slot="body"
      data-variant={variant}
      className={cn(bodyVariants({ variant }), className)}
      {...props}
    />
  )
}

export { Body, bodyVariants }
```

</Tabs.Tab>

<Tabs.Tab>

### Body

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"default" \| "compact"` | `"default"` | `default` renders as `<p>` (16px), `compact` renders as `<small>` (14.22px). |
| `asChild` | `boolean` | `false` | Merges props onto the child element instead of rendering the default tag. |
| `className` | `string` | — | Additional CSS classes to merge. |

The `Body` component also accepts all standard HTML attributes.

</Tabs.Tab>

</Tabs>
