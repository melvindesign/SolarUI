---
title: Input
description: Displays a form input field or a component that looks like an input field.
---

import { Tabs } from 'nextra/components'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'
import { Label } from '@/components/ui/label'
import { ComponentPreview, ComponentPreviewRow } from '@/components/component-preview'


# Input

Displays a form input field or a component that looks like an input field.

<ComponentPreview>
  <Input className="max-w-sm" placeholder="Email" type="email" />
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
import { Input } from "@/components/ui/input"
```

```tsx
<Input placeholder="Email" type="email" />
```

## Examples

### Default

<ComponentPreview>
  <Input className="max-w-sm" placeholder="Email" />
</ComponentPreview>

```tsx
<Input placeholder="Email" />
```

### With label

<ComponentPreview>
  <div className="grid w-full max-w-sm gap-1.5">
    <Label htmlFor="email">Email</Label>
    <Input type="email" id="email" placeholder="name@example.com" />
  </div>
</ComponentPreview>

```tsx
<div className="grid w-full max-w-sm gap-1.5">
  <Label htmlFor="email">Email</Label>
  <Input type="email" id="email" placeholder="name@example.com" />
</div>
```

### With placeholder

<ComponentPreview>
  <Input className="max-w-sm" placeholder="Search..." />
</ComponentPreview>

```tsx
<Input placeholder="Search..." />
```

### Disabled

<ComponentPreview>
  <Input className="max-w-sm" placeholder="Disabled input" disabled />
</ComponentPreview>

```tsx
<Input placeholder="Disabled input" disabled />
```

### With button

<ComponentPreview>
  <div className="flex w-full max-w-sm gap-2">
    <Input placeholder="Search..." />
    <Button type="submit">Search</Button>
  </div>
</ComponentPreview>

```tsx
<div className="flex w-full max-w-sm gap-2">
  <Input placeholder="Search..." />
  <Button type="submit">Search</Button>
</div>
```

### File

<ComponentPreview>
  <div className="grid w-full max-w-sm gap-1.5">
    <Label htmlFor="file">Upload file</Label>
    <Input id="file" type="file" />
  </div>
</ComponentPreview>

```tsx
<div className="grid w-full max-w-sm gap-1.5">
  <Label htmlFor="file">Upload file</Label>
  <Input id="file" type="file" />
</div>
```

### Password

<ComponentPreview>
  <Input className="max-w-sm" type="password" placeholder="Password" />
</ComponentPreview>

```tsx
<Input type="password" placeholder="Password" />
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add https://solar-ui.com/r/input.json
```

---

### Manual

<Tabs items={['CLI', 'Manual']}>
  <Tabs.Tab>

```bash filename="terminal" copy
npx shadcn@latest add input
```

  </Tabs.Tab>
  <Tabs.Tab>

**1. Copy and paste the following code into your project:**

```tsx filename="components/ui/input.tsx" copy
import * as React from "react"

import { cn } from "@/lib/utils"

function Input({ className, type, ...props }: React.ComponentProps<"input">) {
  return (
    <input
      type={type}
      data-slot="input"
      className={cn(
        "bg-default-3 border-default-7 focus-visible:border-default-8 focus-visible:ring-default-7 aria-invalid:ring-error-7 aria-invalid:border-error-8 disabled:bg-default-3 h-8 rounded-lg border bg-transparent px-2.5 py-1 text-base transition-colors file:h-6 file:text-sm file:font-medium focus-visible:ring-3 aria-invalid:ring-3 md:text-sm w-full min-w-0 outline-none file:inline-flex file:border-0 file:bg-transparent file:text-default-12 placeholder:text-default-11 disabled:pointer-events-none disabled:cursor-not-allowed disabled:opacity-50",
        className
      )}
      {...props}
    />
  )
}

export { Input }
```

  </Tabs.Tab>
</Tabs>

</Tabs.Tab>

<Tabs.Tab>

### Input

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `string` | `"text"` | The type of the input (e.g. `text`, `email`, `password`, `file`). |
| `placeholder` | `string` | — | Placeholder text shown when the field is empty. |
| `disabled` | `boolean` | `false` | When `true`, prevents user interaction and applies reduced opacity. |
| `className` | `string` | — | Additional CSS classes to merge. |

The `Input` component accepts all standard HTML `<input>` attributes.

</Tabs.Tab>

</Tabs>
