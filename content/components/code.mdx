---
title: Code
description: Inline and compact monospace text for code snippets.
---

import { Code } from '@/components/ui/code'
import { ComponentPreview } from '@/components/component-preview'
import { Tabs } from 'nextra/components'

# Code

Monospace text for inline code references. Renders as a `<code>` tag by default.

<ComponentPreview>
  <div className="flex flex-col gap-3">
    <p className="text-sm text-default-12">Use <Code>npm install</Code> to install dependencies.</p>
    <p className="text-sm text-default-12">Import with <Code variant="compact">import &#123; Code &#125; from "@/components/ui/code"</Code></p>
  </div>
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
import { Code } from "@/components/ui/code"
```

```tsx
<p>Run <Code>npm install</Code> to get started.</p>
```

## Examples

### Default vs Compact

<ComponentPreview>
  <div className="flex flex-col gap-3">
    <div>
      <p className="text-xs text-default-11 mb-1">Default (16px)</p>
      <p className="text-base text-default-12">The <Code>useState</Code> hook manages local state.</p>
    </div>
    <div>
      <p className="text-xs text-default-11 mb-1">Compact (12.44px)</p>
      <p className="text-sm text-default-12">Pass <Code variant="compact">asChild</Code> to override the rendered tag.</p>
    </div>
  </div>
</ComponentPreview>

```tsx
<Code>useState</Code>
<Code variant="compact">asChild</Code>
```

### Custom element with asChild

<ComponentPreview>
  <Code asChild>
    <kbd>Cmd+K</kbd>
  </Code>
</ComponentPreview>

```tsx
<Code asChild>
  <kbd>Cmd+K</kbd>
</Code>
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add https://solar-ui.com/r/code.json
```

---

### Manual

Copy and paste the following code into your project:

```tsx filename="components/ui/code.tsx" copy
import { cva, type VariantProps } from "class-variance-authority"
import { Slot } from "radix-ui"
import * as React from "react"

import { cn } from "@/lib/utils"

const codeVariants = cva(
  "font-mono text-default-12",
  {
    variants: {
      variant: {
        default: "text-code font-normal",
        compact: "text-code-compact font-normal",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
)

type CodeProps = React.HTMLAttributes<HTMLElement> &
  VariantProps<typeof codeVariants> & { asChild?: boolean }

function Code({ className, variant = "default", asChild = false, ...props }: CodeProps) {
  const Comp = asChild ? Slot.Root : "code"
  return (
    <Comp
      data-slot="code"
      data-variant={variant}
      className={cn(codeVariants({ variant }), className)}
      {...props}
    />
  )
}

export { Code, codeVariants }
```

</Tabs.Tab>

<Tabs.Tab>

### Code

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"default" \| "compact"` | `"default"` | `default` is 16px, `compact` is 12.44px. Both use Geist Mono. |
| `asChild` | `boolean` | `false` | Merges props onto the child element instead of rendering a `<code>`. |
| `className` | `string` | — | Additional CSS classes to merge. |

The `Code` component also accepts all standard HTML attributes.

</Tabs.Tab>

</Tabs>
