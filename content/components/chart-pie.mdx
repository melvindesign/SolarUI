---
title: Pie Chart
description: A circular chart divided into slices, useful for showing proportions within a whole.
---

import { PieChartDefault, PieChartDonut } from '@/components/charts/pie-chart'
import { ComponentPreview } from '@/components/component-preview'
import { Tabs } from 'nextra/components'

# Pie Chart

A circular chart divided into slices, useful for showing proportions within a whole.

<ComponentPreview>
  <PieChartDefault />
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
"use client"

import { Pie, PieChart, Cell } from "recharts"
import {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
} from "@/components/ui/chart"
```

```tsx
const chartData = [
  { browser: "chrome", visitors: 275 },
  { browser: "safari", visitors: 200 },
]

const chartConfig = {
  chrome: { label: "Chrome", color: "var(--chart-1)" },
  safari: { label: "Safari", color: "var(--chart-2)" },
}

<ChartContainer config={chartConfig} className="h-64 w-full">
  <PieChart>
    <ChartTooltip content={<ChartTooltipContent nameKey="browser" hideLabel />} />
    <Pie data={chartData} dataKey="visitors" nameKey="browser" outerRadius={90}>
      {chartData.map((entry) => (
        <Cell key={entry.browser} fill={`var(--color-${entry.browser})`} />
      ))}
    </Pie>
  </PieChart>
</ChartContainer>
```

## Examples

### Donut

<ComponentPreview>
  <PieChartDonut />
</ComponentPreview>

```tsx
<Pie
  data={chartData}
  dataKey="visitors"
  innerRadius={50}
  outerRadius={90}
>
  ...
</Pie>
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add http://localhost:3000/r/chart-pie.json
```

---

### Manual

**1. Install the dependency:**

```bash filename="terminal" copy
npm install recharts
```

**2. Copy `components/ui/chart.tsx` into your project.**

</Tabs.Tab>

<Tabs.Tab>

| Prop | Type | Description |
|------|------|-------------|
| `dataKey` | `string` | The key in data objects to use as slice value. |
| `nameKey` | `string` | The key to use as slice label. |
| `innerRadius` | `number` | Set to create a donut chart. |
| `outerRadius` | `number` | Outer radius in pixels. |
| `cx / cy` | `string \| number` | Center position. Defaults to `"50%"`. |

</Tabs.Tab>

</Tabs>
