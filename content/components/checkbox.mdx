---
title: Checkbox
description: A control that allows the user to toggle between checked, unchecked, and indeterminate states.
---

import { ComponentPreview, ComponentPreviewRow } from '@/components/component-preview'
import { Checkbox } from '@/components/ui/checkbox'
import { Field, FieldContent, FieldDescription, FieldError, FieldLabel, FieldLegend, FieldSet } from '@/components/ui/field'
import { Label } from '@/components/ui/label'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { Tabs } from 'nextra/components'

# Checkbox

A control that allows the user to toggle between checked, unchecked, and indeterminate states.

<ComponentPreview>
  <div className="flex items-center gap-2">
    <Checkbox id="terms" />
    <Label htmlFor="terms">Accept terms and conditions</Label>
  </div>
</ComponentPreview>

<Tabs items={['Usage', 'Installation', 'API']}>

<Tabs.Tab>

```tsx
import { Checkbox } from "@/components/ui/checkbox"
```

```tsx
<Checkbox />
```

## Examples

### With label

<ComponentPreview>
  <div className="flex items-center gap-2">
    <Checkbox id="demo-label" defaultChecked />
    <Label htmlFor="demo-label">Subscribe to newsletter</Label>
  </div>
</ComponentPreview>

```tsx
<div className="flex items-center gap-2">
  <Checkbox id="newsletter" defaultChecked />
  <Label htmlFor="newsletter">Subscribe to newsletter</Label>
</div>
```

### Disabled

<ComponentPreviewRow>
  <div className="flex items-center gap-2">
    <Checkbox id="disabled-unchecked" disabled />
    <Label htmlFor="disabled-unchecked" className="opacity-50">Unchecked</Label>
  </div>
  <div className="flex items-center gap-2">
    <Checkbox id="disabled-checked" disabled defaultChecked />
    <Label htmlFor="disabled-checked" className="opacity-50">Checked</Label>
  </div>
</ComponentPreviewRow>

```tsx
<Checkbox disabled />
<Checkbox disabled defaultChecked />
```

### Group

<ComponentPreview>
  <FieldSet>
    <FieldLegend>Notifications</FieldLegend>
    <div className="flex flex-col gap-2">
      <div className="flex items-center gap-2">
        <Checkbox id="notif-email" defaultChecked />
        <Label htmlFor="notif-email">Email</Label>
      </div>
      <div className="flex items-center gap-2">
        <Checkbox id="notif-push" defaultChecked />
        <Label htmlFor="notif-push">Push</Label>
      </div>
      <div className="flex items-center gap-2">
        <Checkbox id="notif-sms" />
        <Label htmlFor="notif-sms">SMS</Label>
      </div>
    </div>
  </FieldSet>
</ComponentPreview>

```tsx
<FieldSet>
  <FieldLegend>Notifications</FieldLegend>
  <div className="flex flex-col gap-2">
    <div className="flex items-center gap-2">
      <Checkbox id="email" defaultChecked />
      <Label htmlFor="email">Email</Label>
    </div>
    <div className="flex items-center gap-2">
      <Checkbox id="push" defaultChecked />
      <Label htmlFor="push">Push</Label>
    </div>
    <div className="flex items-center gap-2">
      <Checkbox id="sms" />
      <Label htmlFor="sms">SMS</Label>
    </div>
  </div>
</FieldSet>
```

### With description

<ComponentPreview>
  <Field orientation="horizontal">
    <Checkbox id="desc-marketing" />
    <FieldContent>
      <FieldLabel htmlFor="desc-marketing">Marketing emails</FieldLabel>
      <FieldDescription>Receive emails about new products, features, and more.</FieldDescription>
    </FieldContent>
  </Field>
</ComponentPreview>

```tsx
<Field orientation="horizontal">
  <Checkbox id="marketing" />
  <FieldContent>
    <FieldLabel htmlFor="marketing">Marketing emails</FieldLabel>
    <FieldDescription>Receive emails about new products, features, and more.</FieldDescription>
  </FieldContent>
</Field>
```

### Invalid

<ComponentPreview>
  <Field orientation="horizontal" data-invalid>
    <Checkbox id="invalid-terms" aria-invalid />
    <FieldContent>
      <FieldLabel htmlFor="invalid-terms">Accept terms and conditions</FieldLabel>
      <FieldError>You must accept the terms to continue.</FieldError>
    </FieldContent>
  </Field>
</ComponentPreview>

```tsx
<Field orientation="horizontal" data-invalid>
  <Checkbox id="terms" aria-invalid />
  <FieldContent>
    <FieldLabel htmlFor="terms">Accept terms and conditions</FieldLabel>
    <FieldError>You must accept the terms to continue.</FieldError>
  </FieldContent>
</Field>
```

### Table

<ComponentPreview>
  <Table>
    <TableHeader>
      <TableRow>
        <TableHead className="w-10"><Checkbox /></TableHead>
        <TableHead>Name</TableHead>
        <TableHead>Email</TableHead>
        <TableHead>Role</TableHead>
      </TableRow>
    </TableHeader>
    <TableBody>
      <TableRow>
        <TableCell><Checkbox /></TableCell>
        <TableCell>Alice Martin</TableCell>
        <TableCell>alice@example.com</TableCell>
        <TableCell>Admin</TableCell>
      </TableRow>
      <TableRow>
        <TableCell><Checkbox defaultChecked /></TableCell>
        <TableCell>Bob Johnson</TableCell>
        <TableCell>bob@example.com</TableCell>
        <TableCell>Member</TableCell>
      </TableRow>
      <TableRow>
        <TableCell><Checkbox /></TableCell>
        <TableCell>Carol White</TableCell>
        <TableCell>carol@example.com</TableCell>
        <TableCell>Viewer</TableCell>
      </TableRow>
    </TableBody>
  </Table>
</ComponentPreview>

```tsx
<Table>
  <TableHeader>
    <TableRow>
      <TableHead className="w-10"><Checkbox /></TableHead>
      <TableHead>Name</TableHead>
      <TableHead>Email</TableHead>
      <TableHead>Role</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><Checkbox /></TableCell>
      <TableCell>Alice Martin</TableCell>
      <TableCell>alice@example.com</TableCell>
      <TableCell>Admin</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><Checkbox defaultChecked /></TableCell>
      <TableCell>Bob Johnson</TableCell>
      <TableCell>bob@example.com</TableCell>
      <TableCell>Member</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><Checkbox /></TableCell>
      <TableCell>Carol White</TableCell>
      <TableCell>carol@example.com</TableCell>
      <TableCell>Viewer</TableCell>
    </TableRow>
  </TableBody>
</Table>
```

### Card

<ComponentPreview>
  <div className="grid w-full max-w-sm gap-3">
    <FieldLabel>
      <Field orientation="horizontal">
        <Checkbox id="card-email" defaultChecked />
        <FieldContent>
          <FieldLabel htmlFor="card-email">Email notifications</FieldLabel>
          <FieldDescription>Get notified when someone mentions you.</FieldDescription>
        </FieldContent>
      </Field>
    </FieldLabel>
    <FieldLabel>
      <Field orientation="horizontal">
        <Checkbox id="card-push" />
        <FieldContent>
          <FieldLabel htmlFor="card-push">Push notifications</FieldLabel>
          <FieldDescription>Receive push alerts on your devices.</FieldDescription>
        </FieldContent>
      </Field>
    </FieldLabel>
  </div>
</ComponentPreview>

```tsx
<div className="grid gap-3">
  <FieldLabel>
    <Field orientation="horizontal">
      <Checkbox id="email" defaultChecked />
      <FieldContent>
        <FieldLabel htmlFor="email">Email notifications</FieldLabel>
        <FieldDescription>Get notified when someone mentions you.</FieldDescription>
      </FieldContent>
    </Field>
  </FieldLabel>
  <FieldLabel>
    <Field orientation="horizontal">
      <Checkbox id="push" />
      <FieldContent>
        <FieldLabel htmlFor="push">Push notifications</FieldLabel>
        <FieldDescription>Receive push alerts on your devices.</FieldDescription>
      </FieldContent>
    </Field>
  </FieldLabel>
</div>
```

</Tabs.Tab>

<Tabs.Tab>

### Via CLI

```bash filename="terminal" copy
npx shadcn@latest add @solar-ui/checkbox
```

---

### Manual

**1. Install the following dependency:**

```bash filename="terminal" copy
npm install @radix-ui/react-checkbox
```

**2. Copy and paste the following code into your project:**

```tsx filename="components/ui/checkbox.tsx" copy
"use client";

import { Checkbox as CheckboxPrimitive } from "radix-ui";
import * as React from "react";

import { cn } from "@/lib/utils";
import { CheckIcon } from "lucide-react";

function Checkbox({
  className,
  ...props
}: React.ComponentProps<typeof CheckboxPrimitive.Root>) {
  return (
    <CheckboxPrimitive.Root
      data-slot="checkbox"
      className={cn(
        `border-default-7 
        aria-checked:bg-brand-9 aria-checked:text-brand-1 aria-checked:border-brand-9 
        aria-invalid:aria-checked:border-brand-9 aria-invalid:border-error-8 aria-invalid:ring-error-7 aria-invalid:ring-3
        focus-visible:border-default-8 focus-visible:ring-default-7 focus-visible:ring-3
        flex size-4 items-center justify-center rounded-[4px] border transition-colors group-has-disabled/field:opacity-50 peer relative shrink-0 outline-none 
        after:absolute after:-inset-x-3 after:-inset-y-2 
        disabled:cursor-not-allowed disabled:opacity-50`,
        className,
      )}
      {...props}
    >
      <CheckboxPrimitive.Indicator
        data-slot="checkbox-indicator"
        className="[&>svg]:size-3.5 grid place-content-center text-current transition-none"
      >
        <CheckIcon />
      </CheckboxPrimitive.Indicator>
    </CheckboxPrimitive.Root>
  );
}

export { Checkbox };
```

</Tabs.Tab>

<Tabs.Tab>

### Checkbox

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean \| "indeterminate"` | — | Controlled checked state. |
| `defaultChecked` | `boolean` | `false` | Initial checked state (uncontrolled). |
| `onCheckedChange` | `(checked: boolean \| "indeterminate") => void` | — | Callback when state changes. |
| `disabled` | `boolean` | `false` | Prevents interaction and applies reduced opacity. |
| `required` | `boolean` | `false` | Makes the checkbox required in a form. |
| `name` | `string` | — | Name submitted with a form. |
| `value` | `string` | `"on"` | Value submitted with a form. |

</Tabs.Tab>

</Tabs>
